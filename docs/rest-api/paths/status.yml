get:
  summary: Get API server status
  description:
    "**NOTE:** This API deleted in version `2.10`."
  tags:
    - status
  deprecated: true
  responses:
    200:
      description: Response when Success
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                example: operational
            required:
              - status
