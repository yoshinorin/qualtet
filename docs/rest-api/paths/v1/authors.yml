get:
  summary: Get all authors
  description: |
    Retrieves a list of all available authors. The response includes each author's details.
  tags:
    - Authors
  responses:
    200:
      description: Response when Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schemas/response/author.yml'
