get:
  summary: Get Content-Types
  description: |
    Retrieves all available content-types. The response includes each content-type's details.
  tags:
    - Content-Types
  responses:
    200:
      description: Response when Success
      content:
        application/json:
          schema:
            items:
              $ref: '../../components/schemas/response/contentType.yml'
