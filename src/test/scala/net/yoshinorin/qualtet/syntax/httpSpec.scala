package net.yoshinorin.qualtet.syntax

import org.scalatest.wordspec.AnyWordSpec
// import akka.http.scaladsl.model.headers.{Referer, `User-Agent`}
// import akka.http.scaladsl.model.Uri
// import akka.http.scaladsl.model.HttpHeader

// testOnly net.yoshinorin.qualtet.syntax.httpSpec
class httpSpec extends AnyWordSpec {

  "http syntax" should {
    "be return None if HttpHeader is empty" in {
      // TODO
      assert(true)
    }
  }

  "http syntax - userAgent" should {
    "be return User-Agent if HttpHeader contains User-Agent" in {
      // TODO
      assert(true)
    }
  }

  "be return Referer if HttpHeader contains Referer" in {
    // TODO
    // val r = Referer(Uri("https://example.com"))
    assert(true)
  }
}
